# 成都网络发令测试档案

- 项目键：`chengdu-network-command`
- 标准项目名：成都网络发令
- 输入别名：成都网络下令
- 业务端：PC 网络发令；不自动等同于网络发令 APP
- 更新日期：2026-09-18（Asia/Shanghai）
- 知识修订：2
- 来源：用户本次补充；数据库结构与部署信息另标观察来源
- 验证状态：业务规则由用户确认；2026-09-18 已使用正值、值长、受令三会话完成一张纯流程票并归档，副值未独立登录

阅读前置：[通用账号与岗位](../network-command-common.md)、[四川管辖关系](../sichuan-jurisdiction.md)。

## CD-ACCOUNT-001：用户指定的当前测试账号

| 测试身份 / 当前岗位参考 | 完整账号 | 单位说明 | 口令引用 |
| --- | --- | --- | --- |
| 调度值长 | `liuchengzhi` | 本次登录显示刘成智；成都地调审核、监护已验证 | `network-command-test-shared` |
| 调度正值 | `chenjing` | 本次登录显示陈静；成都地调制票、下令已验证 | `network-command-test-shared` |
| 调度副值 | `luosanjie` | 用户指定成都地调场景；本次未独立登录核验 | `network-command-test-shared` |
| 受令端 | `sunjie` | 本次登录显示孙杰；配网调控班签收、复诵、回令已验证 | `network-command-test-shared` |

这些是 2026-09-18 用户提供的当前岗位安排，不是账号永久权限。交接班允许选择职位；普通回归不交接班，并在开测时验证岗位。

术语备注：用户本次称“副值”；2026-09-18 只读数据库盘点中角色名称包含“副职”。自动化需要核对实际界面的岗位名称及对应权限，不能仅按文字不同判错或新建角色。

`sunjie` 被指定作为配网调控班受令测试账号；不要由此推断所有受令单位、所有配网场景都能用此账号替代。

## CD-SCOPE-001：主网调度与直接受令单位

- 成都网络发令调度管辖分为主网、配网。
- 用户明确的主网发令单位：`成都地调`。
- 用户提供的直接受令单位查询：

```sql
SELECT SLORGANNAME
FROM SCCDWLXL_OPCARD.TH_DC_DCMS_ORGANDISPREL
WHERE FLORGANNAME = '成都地调';
```

- `FLORGANNAME` 为发令单位名称；`SLORGANNAME` 为受令单位名称。
- 2026-09-18 只读元数据检查还确认该表有 `FLORGANID`、`SLORGANID`、`KEYID` 列。做账号/单据关联时应核对 ID，不仅依赖显示名称；具体业务映射仍需确认。
- 查询只代表当前配置的直接关系，不应冻结成永久单位名单，也不应自动扩展成所有后代单位。

## CD-SCOPE-002：票据调度归属

操作票通过 `areano` 说明由哪个调度管辖；其他业务有类似规则。测试应同时核对发令单位、受令单位、票据所属调度和账号当前岗位。主配混用、多级关系的边界遵循四川通用条目 SC-SCOPE-001 / SC-SCOPE-002。

2026-09-18 实测票 `地调字_2609006` 的 `areano` 为 `1eaa62ea-6985-40f8-8e00-52465722effa`，该次页面业务上下文为成都地调。此为该票及当前环境观察，不代表已验证所有单位 ID 的映射。

## CD-FLOW-001：正值制票、值长审核、受令端完成闭环

用户明确：通常由正值写票、值长审核，值长一般不写票。本次已通过正常页面完成制票、审核预发、受令签收与签到、监护下令、双方复诵确认、回令收令、厂站确认及归档。

详细步骤、实际状态字段、测试边界及校核服务异常处理见 [成都操作票纯流程实测记录](chengdu-operation-ticket-flow.md)。不要用修改数据库状态代替这些业务动作，也不要把本次跳过外部同步或忽略校核服务异常的专项授权扩展到生产环境。

## 环境与数据库线索

- 环境：`192.168.101.14`，见 [环境快照](../environments/101-14.md)。
- 业务 schema：`SCCDWLXL_OPCARD`；平台权限 schema：`SCCDWLXL_PLATFORM`。
- 已观察到平台角色/账号关联表 `T_TBP_ROLE`、`T_TBP_USERROLE`，业务单位表 `TH_DC_DCMS_ORGAN`、管辖表 `TH_DC_DCMS_ORGANDISPREL`。
- 角色定义和账号角色关联不等于用户当前班次岗位；需要核对交接班后的真实状态。

## 后续需补充的信息

- 配网发令单位、配网与主网的具体操作流程差异。
- 副值账号独立登录、各账号完整单位 ID 与岗位权限、同一账号并发登录规则。本次仅验证了三个不同账号的隔离会话，不改变班次。
- 其他单位的 `areano` 编码与调度映射；其他业务对应字段。
- 真实设备校核、交接班、多指令/多受令单位、主配及跨级权限、PMS/OMS 联调等扩展场景。
- 后续批量测试的数据命名、保留和清理策略；本次已归档的纯流程测试票保留审计记录，不删除。

这些缺项不妨碍记录知识或制定初步方案，但不能据此声称已经具备完整业务回归覆盖。

## 来源记录

- 2026-09-18：用户给定四个账号、当前岗位、配网调控班受令账号、交接班动态选岗、成都地调受令查询及 `areano` 含义。
- 2026-09-18：此前只读盘点确认角色名称和关系表列、数据库 schema 与部署路径；未修改数据、未验证登录。
- 2026-09-18：用户授权在 14 环境创建纯流程测试票，限定仅验证内部流程、跳过外部同步，并允许忽略本次校核服务异常继续。三账号页面操作完成，DM 只读核验归档状态；未切换岗位、未关闭防误校核开关、未直接执行数据库写入。

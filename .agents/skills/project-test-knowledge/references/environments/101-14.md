# 101.14 测试环境快照

来源：2026-09-18 SSH 与 DM 插件只读盘点。以下运行状态只对盘点时刻有效，不代表随时可测试。

- 主机：`192.168.101.14`；SSH 端口 `20020`。
- 系统：Ubuntu 24.04.1，16 核，约 128GB 内存。
- DM：`192.168.101.14:15236`；本机已有 DM 插件连接名称 `192.168.101.14`。
- 未保存 SSH 或数据库密码到本知识库。

| 项目 | 部署目录 | 容器 | 应用端口 / 上下文 | 当时状态 |
| --- | --- | --- | --- | --- |
| 成都网络发令 | `/home/czp/netorder/cd/netorder-docker` | `th-netorder-cd` | `28096/netorder/` | 主应用停止，认证服务运行 |
| 南充网络发令 | `/home/czp/netorder/nc/netorder-docker` | `th-netorder-nc` | `28116/netorder/` | 主应用停止，认证服务运行 |
| 四川省调网络发令 | `/home/czp/netorder/sd/netorder-docker` | `th-netorder-sd` | `28086/netorder/` | 运行，入口 GET 返回 200 |
| 阿坝网络发令 | `/home/czp/netorder/ab/netorder-docker` | `th-netorder-ab` | `28106/netorder/` | 运行，入口 GET 返回 200 |

## 后续测试设计依据

- 多项目共用 DM 实例，分别存在业务和平台权限 schema。隔离和清理不能针对整个共享实例。
- 部署涉及业务 JAR、许可证、配置、部分项目的 Word 模板，以及认证、CAS、Zookeeper 等依赖。
- 当前部分镜像为 `latest`，Dockerfile 使用 `COPY *.jar`。未来自动部署需明确固定输入包、提交和哈希，不能从目录随意选“最新”文件。
- 未来测试应按项目与环境互斥，防止正在回归时被另一任务覆盖版本。
- 本文件不授权重启、部署、数据库迁移、登录或业务写入；相关操作由当次任务决定。
- HTTP 200、容器运行和表存在均不等于登录成功或业务测试通过。
